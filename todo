
// MLX uses fourth value for transperency, might be issue later, when colour dont look exactly as they should

//free stuff
//definatelly added player to struct without freeing, check later
//ft_strtrim uses malloc >> needs to be freed

//parsing map
// if too many new lines after map, issue
// check if all textures present - otherwise error
// if texture files work, check maybe later when opening them